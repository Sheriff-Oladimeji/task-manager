import React from "react";
import { Calendar } from "./ui/calendar";
const Scheduler = () => {
  return (
    <div className="row-span-2 bg-accent">
      <Calendar className="bg-card" />
      Hello
    </div>
  );
};

export default Scheduler;
